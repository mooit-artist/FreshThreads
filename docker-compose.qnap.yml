# Fresh Threads LLC - QNAP HTTPS-only Deployment
# Docker Compose configuration for QNAP with HTTPS-only support

version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.ssl
    container_name: freshthreads-frontend-qnap
    restart: unless-stopped
    ports:
      - '18080:443' # HTTPS only - no HTTP port exposed
    networks:
      - freshthreads-network
    depends_on:
      - backend
    environment:
      - NODE_ENV=production
    volumes:
      - ./logs/nginx:/var/log/nginx
    healthcheck:
      test: ['CMD', 'curl', '-f', '-k', 'https://localhost:443/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend-ssl
    container_name: freshthreads-backend-qnap
    restart: unless-stopped
    ports:
      - '18444:8443' # HTTPS Backend API
    networks:
      - freshthreads-network
    environment:
      - PRINTIFY_API_KEY=${PRINTIFY_API_KEY}
      - PRINTIFY_SHOP_ID=${PRINTIFY_SHOP_ID}
      - FLASK_ENV=production
    volumes:
      - ./logs/backend:/app/logs
    healthcheck:
      test: ['CMD', 'curl', '-f', '-k', 'https://localhost:8443/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  freshthreads-network:
    driver: bridge
    name: freshthreads-qnap-network

volumes:
  nginx_logs:
    driver: local
  backend_logs:
    driver: local
